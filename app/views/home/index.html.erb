<h1 class="is-size-2">Popular Players</h1>
<div class="players">
	<% @popular_players.each do |player| %>
		<%= puts player.id %>
		<%= render partial: "partials/player", locals: {player: player} %>
	<% end %>
</div>
